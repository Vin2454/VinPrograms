using System;
using System.Collections.Generic;
using System.Text;

namespace VinPrograms.Arrays
{
    public class MinimumDifference
    {
        public void PrintMinimumDifference()
        {
            int[] a = {23,5,10,17,30 };
            int[] b = { 26, 134, 135, 14, 19 };
            //Expected output: [17,19]

            //question: implement a function that takes in two non-empty arrays of integers, finds the pair of numbers 
            //(one from each array) whose absolute difference is closest to zero and returns a pair containing 
            //these two numbers, with the first number from array. Only one such pair will exist for the test.

        }
    }
}
